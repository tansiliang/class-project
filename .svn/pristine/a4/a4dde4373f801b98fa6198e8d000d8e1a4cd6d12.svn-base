$("document").ready(function(){
    var arr = Array('admin_list_a','admin_add_a','font_list_a','font_add_a','work_list_a','work_add_a','naviga_list_a','naviga_add_a','website_list','switchble','forum','discuss','reply','addresslist','album','album_photo','arrondi');

    //左侧导航列表
    $("#admin_list_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        //key下标、value内容
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    $("#admin_add_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
        });
    });
    $("#font_list_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    $("#font_add_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    //文章列表
    $("#work_list_a").on('click',function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    // 添加文章列表
    $("#work_add_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    // 导航分类列表
    $("#naviga_list_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
        });
    });
    // 添加导航分类
    $("#naviga_add_a").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
            
        });
    });
    //网站设置信息
    $("#website_list").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
            $(vals).removeClass("active");
            }
        });
    });
    //图片轮播管理
    $("#switchble").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //论坛管理
    $("#forum").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //评论管理
    $("#discuss").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //回复管理
    $("#reply").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //通讯录管理
    $("#addresslist").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //相册管理
    $("#album").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //照片管理
    $("#album_photo").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });
    //下载专区
    $("#arrondi").click(function(){
        $(".bread_a").text($(this).text());
        $(".top_span").text($(this).text());
        var id = $(this).attr("id");
        $.each(arr,function(key,value){
            var vals = '#'+value;
            if(value == id){
                $(vals).addClass("active");
            }else{
                $(vals).removeClass("active");
            }
        });
    });

    //首页
    $(".bread_li_a").click(function(){
        $(".bread_a").text("网站信息");
        $(".top_span").text("网站信息");
        $("#admin_add_a").removeClass("active");
        $("#admin_list_a").removeClass("active");
        $("#font_list_a").removeClass("active");
        $("#font_add_a").removeClass("active");
    }); 
});